# GitHub personal access token — required for publishing and installing @mszczygiel-projects packages
# Scopes needed: read:packages (install), write:packages (publish)
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=
